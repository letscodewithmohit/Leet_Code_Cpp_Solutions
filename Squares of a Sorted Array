class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        int n = nums.size();
        vector<int>ans;
        for(auto i : nums){
         ans.push_back(i*i);
        }
        sort(ans.begin(),ans.end());
        return ans;
    }
};
